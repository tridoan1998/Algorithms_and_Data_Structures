//Program to return the sum of all elements in the array.
#include <iostream>
using namespace std;
int main()
{
    int size;
    cout << "Enter the size of the array: ";
    cin >> size;
    int *numberlist;

    numberlist = new int[size];
        delete[] pt;

    int counter = 0;
    while(counter < size)
    {
        int element;
        cout "Enter the element for spot #" size+1 << " in the array: ";
        cin >> element;
        numberlist[counter]
        counter++;
    }
    int sum = 0;
    int numberlist = new array[];
    for (int i = 0; i <= )
    int numberlist[10] = {1, 2, 3};
    int size = *(&numberlist + 1) - numberlist; // Return the size of the array
    for (int i = 0; i <=size; i++)
    {
            sum += a[i];
    }
    cout << sum;
    return 0;


}
